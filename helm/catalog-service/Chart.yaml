apiVersion: v2
name: catalog-service
description: Helm chart for the Fallseed catalog-service (FastAPI product catalogue).
type: application

# Chart version — bump on chart changes.
version: 0.1.0

# Version of the app this chart deploys (matches the image tag default).
appVersion: "0.1.0"

keywords:
  - fallseed
  - catalog
  - fastapi
  - microservice

maintainers:
  - name: Fallseed
