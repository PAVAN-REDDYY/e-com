apiVersion: v2
name: order-service
description: Helm chart for the Fallseed order-service (FastAPI orders + pricing).
type: application

# Chart version — bump on chart changes.
version: 0.1.0

# Version of the app this chart deploys (matches the image tag default).
appVersion: "0.1.0"

keywords:
  - fallseed
  - orders
  - fastapi
  - microservice

maintainers:
  - name: Fallseed
